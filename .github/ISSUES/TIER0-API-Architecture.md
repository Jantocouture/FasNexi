# API Architecture & Routes (Tier 0)

Define API surface and initial route structure.

Acceptance criteria:
- API route skeleton in server/ with domain routes: /api/auth, /api/products, /api/users, /api/style-profile, /api/wardrobe.
- Route-level tests or Postman collection with basic requests documented.

Success metric: API skeleton responds on staging and docs provide example calls.

